# Backend environment example for SecuHire
# Copy to .env and adjust values

# MongoDB
MONGO_URL=mongodb://localhost:27017
DB_NAME=secuhire

# Security
JWT_SECRET=change_me_dev_secret

# LiveKit / Proctoring
LIVEKIT_API_KEY=devkey
LIVEKIT_API_SECRET=devsecret
LIVEKIT_WS_URL=wss://your-domain.com:7880
PUBLIC_BASE_URL=https://your-domain.com
RECORDINGS_DIR=${PWD}/recordings
EGRESS_SERVICE_URL=http://egress:3001

# TURN / STUN (if using external coturn)
TURN_DOMAIN=turn.your-domain.com
TURN_EXTERNAL_IP=YOUR_PUBLIC_IP
TURN_USERNAME=turnuser
TURN_PASSWORD=turnpass
TURN_STATIC_SECRET=changeme_static_secret

# CORS (comma-separated origins). Use * for all in dev only.
ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000,http://10.75.122.198:3000

# AWS (optional, required if enabling S3 storage)
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_REGION=us-east-1
AWS_S3_BUCKET=secuhire-dev-bucket

# Base URLs (used for generating links in emails/webhooks)
BACKEND_BASE_URL=http://localhost:8001
FRONTEND_BASE_URL=http://10.75.122.198:3000

# Storage mode for recordings: local or s3
VIDEO_STORAGE=local

# Jobma integration (optional). If not set, in-house video flow will be used.
JOBMA_API_KEY=
JOBMA_API_BASE=https://api.jobma.com
JOBMA_WEBHOOK_SECRET=

# Recruit CRM integration (optional). If not set, native ATS remains internal.
RECRUITCRM_API_KEY=
RECRUITCRM_API_BASE=https://api.recruitcrm.io

# Feature flags (comma-separated): interviews,telemetry,webhooks,external_integrations
FEATURE_FLAGS=interviews,telemetry,webhooks

# Safe Exam Browser (SEB) enforcement
# When enabled, interview endpoints will require SEB headers.
# Set SEB_CONFIG_KEY_HASH to the Config Key Hash from your SEB configuration (.seb file)
# Docs: https://safeexambrowser.org/
SEB_REQUIRED=false
SEB_CONFIG_KEY_HASH=
