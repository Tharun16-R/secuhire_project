# Backend environment example for SecuHire
# Copy to .env and adjust values

# MongoDB
MONGO_URL=mongodb://localhost:27017
DB_NAME=secuhire

# Security
JWT_SECRET=change_me_dev_secret

# CORS (comma-separated origins). Use * for all in dev only.
ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# AWS (optional, required if enabling S3 storage)
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_REGION=us-east-1
AWS_S3_BUCKET=secuhire-dev-bucket
