# Frontend environment example for SecuHire
# Copy to .env or .env.local and adjust values

# Backend API URL (FastAPI backend on Render)
REACT_APP_API_URL=https://secuhire-project.onrender.com

# Public base URL used to build QR code link for phone join
# For LAN testing from phone, point this to your laptop IP
REACT_APP_PUBLIC_BASE_URL=http://10.75.122.198:3000

# Optional explicit laptop IP override used by QRForPhoneJoin
# REACT_APP_LAPTOP_IP=10.75.122.198

# Optional override for LiveKit WS (normally provided by backend responses)
# REACT_APP_LIVEKIT_WS_URL=wss://your-domain.com:7880

# Firebase public web config (from Firebase Console → Project settings → Your apps → Web app)
# These are safe to be in client env; do not commit secrets.
REACT_APP_FIREBASE_API_KEY=
REACT_APP_FIREBASE_AUTH_DOMAIN=
REACT_APP_FIREBASE_PROJECT_ID=
REACT_APP_FIREBASE_STORAGE_BUCKET=
REACT_APP_FIREBASE_APP_ID=
